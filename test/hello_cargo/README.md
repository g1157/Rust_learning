# Hello Cargo

Rust 入门项目 - 使用 Cargo 构建工具创建的 Hello World 程序。

## 项目简介

这是使用 `cargo new` 命令创建的标准 Rust 项目模板，展示了 Cargo 项目的基本结构。

## 功能

- 打印 "Hello, world!" 到控制台

## 运行

```bash
cd test/hello_cargo
cargo run
```

## 学习要点

- Cargo 项目结构（`src/main.rs`, `Cargo.toml`）
- 使用 `cargo build` 和 `cargo run` 命令
