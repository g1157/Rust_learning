# Hello World

最简单的 Rust 程序 - 不使用 Cargo 的纯 rustc 编译示例。

## 项目简介

这是一个不依赖 Cargo 的最小 Rust 程序，直接使用 `rustc` 编译器编译。

## 功能

- 打印 "Hello, world!" 到控制台

## 编译和运行

```bash
cd test/hello_world
rustc main.rs
./main
```

## 学习要点

- Rust 源文件使用 `.rs` 扩展名
- 使用 `rustc` 直接编译 Rust 代码
- 对比 Cargo 项目与直接编译的区别
