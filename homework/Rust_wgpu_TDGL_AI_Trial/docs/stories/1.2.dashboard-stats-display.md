# Story 1.2: 数据展示与状态面板

> Epic: TDGL Simulator Dashboard - 棕地增强
> 生成时间: 2025-12-25

---

## Status

**Draft**

---

## Story

**As a** 研究者,
**I want** 实时查看涡旋统计、能量变化和时间序列图,
**so that** 我可以在仿真过程中监控系统状态并及时调整参数。

---

## Acceptance Criteria

1. 右侧面板显示实时统计（涡旋数、钉扎率、能量密度、速度）
2. 时间序列图（egui_plot）显示最近 1000 步的涡旋数和能量
3. 底部状态栏显示进度、ETA、GPU 信息、steps/s
4. 涡旋位置叠加显示在热力图上（红色涡旋、青色反涡旋）
5. 鼠标悬停显示坐标和 |ψ| 值
6. 数值变化时有视觉高亮反馈

---

## Tasks / Subtasks

- [ ] **Task 1: 添加 egui_plot 依赖** (AC: 2)
  - [ ] 1.1 在 Cargo.toml 添加 `egui_plot = "0.30"`
  - [ ] 1.2 验证编译通过

- [ ] **Task 2: 右侧统计面板** (AC: 1)
  - [ ] 2.1 创建 `src/ui/panels/stats_panel.rs`
  - [ ] 2.2 实现 `StatCard` 组件（标签 + 大数值 + 单位）
  - [ ] 2.3 添加涡旋统计区（涡旋数、反涡旋数、净涡旋）
  - [ ] 2.4 添加钉扎统计区（钉扎数、钉扎率百分比）
  - [ ] 2.5 添加能量统计区（能量、能量密度）
  - [ ] 2.6 添加速度统计区（vx, vy, |v|）

- [ ] **Task 3: 时间序列图** (AC: 2)
  - [ ] 3.1 创建 `src/ui/components/time_series.rs`
  - [ ] 3.2 实现环形缓冲区存储最近 1000 步数据
  - [ ] 3.3 使用 `egui_plot::Plot` 绘制涡旋数曲线
  - [ ] 3.4 添加能量曲线（双 Y 轴或归一化）
  - [ ] 3.5 添加图例和轴标签

- [ ] **Task 4: 底部状态栏** (AC: 3)
  - [ ] 4.1 创建 `src/ui/panels/status_bar.rs`
  - [ ] 4.2 显示当前步数 / 总步数
  - [ ] 4.3 显示仿真时间
  - [ ] 4.4 实现进度条组件
  - [ ] 4.5 计算并显示 ETA
  - [ ] 4.6 显示 GPU 名称
  - [ ] 4.7 显示实时 steps/s

- [ ] **Task 5: 涡旋叠加显示** (AC: 4)
  - [ ] 5.1 创建 `src/viz/vortex_overlay.rs`
  - [ ] 5.2 获取涡旋位置数据（从现有 `detect_vortices` 结果）
  - [ ] 5.3 使用 egui Painter 绘制涡旋标记（红色实心圆）
  - [ ] 5.4 绘制反涡旋标记（青色空心圆）
  - [ ] 5.5 可选：钉扎涡旋用黄色菱形标记

- [ ] **Task 6: 鼠标悬停信息** (AC: 5)
  - [ ] 6.1 捕获鼠标在热力图区域的位置
  - [ ] 6.2 将屏幕坐标转换为网格坐标
  - [ ] 6.3 读取对应位置的 |ψ| 值
  - [ ] 6.4 在热力图下方显示坐标和数值

- [ ] **Task 7: 数值高亮动画** (AC: 6)
  - [ ] 7.1 创建 `src/utils/animation.rs`
  - [ ] 7.2 实现简单的颜色插值函数
  - [ ] 7.3 在 StatCard 中检测数值变化
  - [ ] 7.4 变化时触发高亮效果（白→青→白，200ms）

---

## Dev Notes

### 依赖 Story 1.1

本故事依赖 Story 1.1 完成的：
- egui 集成基础设施
- `src/ui/` 模块结构
- 深色主题配置

### 现有数据源 (`src/main.rs`)

| 数据 | 来源 | 行号 |
|------|------|------|
| 涡旋位置 | `detect_vortices()` 返回 | ~2800 |
| 涡旋统计 | `VortexStats` 结构体 | ~2750 |
| 能量 | `compute_energy()` | ~2600 |
| 速度 | `mean_vx`, `mean_vy` | ~2780 |

### 配色 (from front-end-spec.md)

```rust
// 涡旋标记色
const VORTEX_COLOR: Color32 = Color32::from_rgb(0xff, 0x6b, 0x6b);      // 红色
const ANTIVORTEX_COLOR: Color32 = Color32::from_rgb(0x4e, 0xcd, 0xc4);  // 青色
const PINNED_COLOR: Color32 = Color32::from_rgb(0xff, 0xe6, 0x6d);      // 黄色

// 高亮色
const HIGHLIGHT_COLOR: Color32 = Color32::from_rgb(0x4f, 0xc3, 0xf7);   // 青色
```

### 文件结构

```
src/
├── ui/
│   ├── panels/
│   │   ├── stats_panel.rs   # 新增
│   │   └── status_bar.rs    # 新增
│   └── components/
│       ├── stat_card.rs     # 新增
│       └── time_series.rs   # 新增
├── viz/
│   └── vortex_overlay.rs    # 新增
└── utils/
    └── animation.rs         # 新增
```

### 环形缓冲区实现

```rust
struct RingBuffer<T, const N: usize> {
    data: [T; N],
    head: usize,
    len: usize,
}
```

---

## Testing

### 验收测试清单

1. [ ] `cargo build` 编译通过
2. [ ] 右侧面板显示所有统计数据
3. [ ] 时间序列图实时更新
4. [ ] 底部状态栏显示正确信息
5. [ ] 涡旋标记正确叠加在热力图上
6. [ ] 鼠标悬停显示坐标和 |ψ| 值
7. [ ] 数值变化时有高亮效果

---

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-12-25 | 0.1 | 初始创建 | Sarah (PO) |

---

## Dev Agent Record

### Agent Model Used

_待开发时填写_

### Debug Log References

_待开发时填写_

### Completion Notes List

_待开发时填写_

### File List

_待开发时填写_

---

## QA Results

_待 QA 审核时填写_
