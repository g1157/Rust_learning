# hk3-4 - 关联函数与构造器

学习 Rust 关联函数（构造器模式）的练习。

## 功能

实现 `TrafficLight` 的关联函数：
- `new()` 构造函数返回新实例
- `get_state()` 方法返回状态引用

## 技术要点

- **关联函数**: 没有 `self` 参数的函数，使用 `::` 调用
- **构造器模式**: `TrafficLight::new()` 创建新实例
- **Self 类型**: 在函数签名和函数体中使用 `Self` 代替具体类型名

## 运行

```bash
cargo run
```
